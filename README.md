# SOS Интернет — clean-premium-v10

Полностью чистая версия проекта.

## Что внутри
- Премиум PWA-сайт.
- Worker API.
- Cloudflare D1 база.
- Админка `/admin`.
- Диагностика `/diagnostics`.
- Telegram webhook.
- Автоматическая выдача после статуса `paid`.

## Важно
Проект сделан строго под Cloudflare Workers + Static Assets.

Важно: в папке `public` нет `_worker.js`, чтобы Wrangler не блокировал деплой.

## Переменные Cloudflare
- `ADMIN_TOKEN` — необязательно, по умолчанию работает `sos_admin_2026_super_secret`
- `TELEGRAM_BOT_TOKEN` — Secret, токен бота из BotFather
- `TELEGRAM_BOT_USERNAME` — Text, username бота без @

## D1 binding
Binding должен называться строго:
`SOS_DB`

## Проверка
- `/api/health`
- `/diagnostics`
- `/admin`

## Webhook
После деплоя открыть:
`/api/telegram/set-webhook?token=sos_admin_2026_super_secret`


## Исправление v11
Удалён `public/_worker.js`, из-за которого Wrangler выдавал ошибку `Uploading a Pages _worker.js file as an asset`.


## Исправление v12
Добавлен `public/.assetsignore` с правилом `_worker.js`, чтобы старый файл `public/_worker.js` не ломал деплой, даже если он остался в GitHub.

## Исправление v13
Автовыдача теперь отправляет не только сообщение, но и отдельный `.txt` документ.
Документ формируется по купленному тарифу:
- personal — личный комплект
- family — семейный комплект
- driver — водитель/курьер
- business — бизнес-комплект

Файл открывается на телефоне и ПК.

## v14 — усиление ценности продукта
Документы выдачи переписаны как полноценные платные комплекты:
- быстрый старт за 15 минут;
- правило 3 минут;
- диагностика сбоя;
- SMS-шаблоны;
- антискам;
- офлайн-данные;
- сценарии по банкам, оплате, мессенджерам, картам;
- расширенные блоки для семьи, водителя и бизнеса;
- поля для заполнения;
- готовые тексты для пересылки.


## v15 — премиальный коммерческий визуал
- Полностью переработан визуал главной страницы.
- Улучшены hero, тарифы, сравнение, FAQ, sticky CTA и блоки преимуществ.
- Функциональность сохранена: PWA, заказы, админка, Telegram-автовыдача, D1.
- Проверка сборки: `/diagnostics` и `/api/health` должны показать `premium-ui-commercial-v15`.


## v16 — sales-pro premium
- Версия сайта: `sales-pro-premium-v16`.
- Обновлён Wrangler до v4.
- Усилен коммерческий визуал и оффер.
- Добавлены блоки: ценность продукта, превью TXT-документа, возражение “это можно найти бесплатно”.
- Сохранены: D1, заказы, админка, Telegram webhook, выдача TXT-документа.
